# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.jpg
server

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dont upload the config.toml
config.toml

# Lockfiles will get uploaded for build or ref, but not the vendor.
vendor/
